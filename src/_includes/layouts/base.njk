<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ title or 'Outer Siraji Preservation' }}</title>
    <link rel="stylesheet" href="{{ eleventy.env.pathPrefix }}assets/styles.css">
    <meta name="color-scheme" content="light dark">
  </head>
  <body>
    <header class="site-header">
      <div class="container">
        <a class="brand" href="{{ eleventy.env.pathPrefix }}">Outer Siraji</a>
        <nav class="nav">
          <a href="{{ eleventy.env.pathPrefix }}">Home</a>
          <a href="{{ eleventy.env.pathPrefix }}dictionary/">Dictionary</a>
          <a href="{{ eleventy.env.pathPrefix }}history/">History</a>
          <a href="{{ eleventy.env.pathPrefix }}works/">Works</a>
        </nav>
      </div>
    </header>
    <main class="container">
      {{ content | safe }}
    </main>
    <footer class="site-footer">
      <div class="container">
        <p>© Outer Siraji Preservation. Content may be licensed separately.</p>
      </div>
    </footer>
  </body>
  </html>


