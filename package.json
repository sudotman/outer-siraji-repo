{
  "name": "outer-siraji-preservation-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prebuild": "node scripts/csv_to_json.mjs && node scripts/build_index.mjs",
    "build": "eleventy",
    "dev": "eleventy --serve --quiet",
    "index": "node scripts/build_index.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "csv-parse": "^5.5.6"
  }
}

